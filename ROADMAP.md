# План дальнейшей разработки

Состояние на момент фиксации: фазы 0-5 закрыты (конфиг + мультивендорный GPU,
обработка ошибок, автотесты + CI, общий backend `Assistant`, три интерфейса
TUI/GUI/консоль, barge-in, упаковка в pip-пакет). Далее — ветка
`v0.2-live-core` (см. «Статус» ниже). 93 автотеста, чистый ruff.

Главная цель следующего этапа — **live talk**: разговор без ощутимых пауз,
а не «нажал, подождал, услышал». Всё остальное подчинено ей. Уточнение цели
после ревизии 28.08 — см. раздел «Ревизия после разбора плана»: сжать
задержку до <1с нельзя, поэтому цель — чтобы пауза не *ощущалась*.

---

## Статус: ветка `v0.2-live-core` (2026-08-28)

Сделано:

- **Бенчмарк** (`gogi-bench`, `scripts/bench_pipeline.py`) — раздел 0 ниже
  переписан по замерам. Главный итог: STT на GPU не нужен, узкое место
  переехало в LLM TTFT.
- **STT-дефолт** `medium -> small` (`config.example.yaml`).
- **События вместо print** (задача 2): `events.py` (EventEmitter/Event),
  ядро больше не печатает. Консоль и TUI подписаны; в TUI ответ стримится
  по токенам в `#streaming`, ошибки и вызовы инструментов — в транскрипт.
- **Машина состояний**: `state.py` (IDLE/LISTENING/TRANSCRIBING/THINKING/
  EXECUTING_TOOL/SPEAKING/ERROR), индикатор в подзаголовке TUI.
- **Ранняя нарезка первой фразы**: `StreamingSpeaker` отдаёт первый кусок
  ответа по запятой/пределу длины, не дожидаясь точки — бьёт по LLM TTFT.

93 автотеста, чистый ruff.

Осталось в этапе / дальше: настоящий отменяемый barge-in (задача 3),
VAD-эндпоинтинг (задача 7) — теперь как UX, не как ускорение STT; оценка
меньшей LLM / отключения reasoning у gpt-oss; таймауты Ollama (задача 4).

---

## Ревизия после разбора плана (2026-08-28, поздняя)

Прошёл допрос по плану. Зафиксированные решения:

1. **Цель этапа переформулирована.** «Отклик < 1 c» недостижим этим
   пайплайном — пол задержки до первого звука ответа 1.6–4.3 c (раздел 0).
   Отказ осознанный: голос важнее скорости, поэтому `tts.nfe_step` остаётся
   **32** (не роняем до 8 ради латентности). Новая цель этапа —
   **убрать мёртвую тишину между репликой пользователя и первым звуком
   ответа**, а не саму задержку. Как — задача 9.
2. **Gogi — личный инструмент на одном стенде** (Ryzen 5600 + RX 9060 XT,
   ROCm). «Продукт для других» и мультивендорность — вне этого этапа,
   вернёмся позже. Следствия — в разделе 1.
3. **Приоритет задач 3 и 5 поднят до 1.** Barge-in без отставания и
   ограничение контекста — обоснование внутри разделов.
4. **Новые задачи этапа:** 9 (маскировка задержки + осмысленный филлер),
   10 (расширить LLM-замер: холодный TTFT, путь с tool-call, p95),
   11 (gpt-oss без reasoning / модель поменьше),
   12 (тест распознавания на наборе реальных команд),
   13 (прогрев моделей на старте).

Новый рекомендуемый порядок — в конце файла.

---

## 0. Бюджет задержки — что реально мешает live talk

Полный цикл реплики складывается из:

```
[запись] -> STT -> LLM (до первого токена) -> TTS (первая фраза) -> звук
```

Измеренное на стенде (AMD RX 9060 XT, ROCm), после прогрева моделей,
на тестовом аудио 9.61с:

| Этап | Где считается | Время | RTF |
|---|---|---|---|
| **STT, whisper `medium` int8** | **CPU** | **5.86с** | **0.61x** |
| TTS, `nfe_step=32` (дефолт) | GPU (ROCm) | ~1.7с на фразу | — |
| TTS, `nfe_step=16` | GPU (ROCm) | ~0.9с | — |
| TTS, `nfe_step=8` | GPU (ROCm) | ~0.45с | — |
| LLM (gpt-oss:20b, до первого токена) | GPU (Ollama) | не замерялось | — |

Разовые издержки старта (в бюджет реплики не входят, но влияют на время
запуска приложения): загрузка whisper `medium` ~3.1с, первый вызов F5-TTS
~7с (компиляция GPU-ядер).

**Первоначальная гипотеза (по `medium`):** узкое место — STT на CPU, оно
обязано переехать на GPU, иначе отклик меньше секунды недостижим.

### Замер опроверг гипотезу — `scripts/bench_pipeline.py`, 2026-08-28

Оформлен воспроизводимый бенчмарк (`gogi-bench`). Прогон на том же стенде
(AMD RX 9060 XT, ROCm), то же аудио 9.61с, медиана из 3 замеров:

| Ступень | Устройство | Время | RTF | CER |
|---|---|---|---|---|
| STT `base` int8 | CPU | **0.51с** | 0.05x | 3.2% |
| STT `small` int8 | CPU | **1.79с** | 0.19x | 1.1% |
| STT `medium` int8 | CPU | 5.79с | 0.60x | 0.0% |
| LLM gpt-oss:20b, TTFT (тёплая) | GPU | 0.8–3.5с | — | — |
| LLM gpt-oss:20b, TTFT (первый вызов) | GPU | ~7с | — | — |
| TTS `nfe_step=8` | GPU | 0.40с | — | — |
| TTS `nfe_step=16` | GPU | 0.76с | — | — |
| TTS `nfe_step=32` | GPU | 1.52с | — | — |

**Новый вывод:** STT на GPU НЕ нужен для цели «STT < 1 с». Модель `base` на
CPU распознаёт весь клип 9.6с за полсекунды при CER 3.2% на живой русской
речи (для коротких команд ошибок практически не будет); `small` — 1.8с при
CER 1.1%. Достаточно **сменить дефолт `stt.model_size` на `base` или
`small`** и подрезать вход VAD-эндпоинтингом. Задача 1 (STT на GPU)
понижается в приоритете: NVIDIA-строчка (1a) остаётся как дешёвый долг,
абстракция бэкендов под AMD (1b) — откладывается до появления реальной
потребности.

**Куда переехало узкое место:** теперь доминирует и сильнее всего скачет
**LLM time-to-first-token** (0.8–3.5с у gpt-oss:20b, а на пути с tool-call —
до ~2–3с молчания перед голосом). Отсюда приоритеты смещаются на:
агрессивную нарезку первой фразы в синтез («Секунду…» уходит в TTS сразу),
события/стриминг текста в UI, и — отдельной задачей — оценку меньшей LLM
или отключения reasoning.

Свежие JSON-отчёты бенчмарка — в `benchmarks/` (в .gitignore), зафиксирован
`benchmarks/baseline.json`.

---

## 1. STT на GPU — ~~главный блокер~~ понижено (было: приоритет 1)

> **Статус (28.08.2026, ветка `v0.2-live-core`).** Замер (раздел 0) снял
> этот пункт с приоритета. `small` на CPU — 1.8с / CER 1.1% на клипе 9.6с
> (на реальной команде 2-3с — доли секунды), `base` — 0.5с. Цель «STT < 1с»
> достигается сменой модели, без GPU. Дефолт `stt.model_size` уже `small`
> (задача 1c закрыта). Осталось из этого раздела:
> - **1a (NVIDIA)** — по-прежнему стоит сделать как дешёвый долг: одна ветка
>   в `stt_engine.py` по `gpu.detect_backend()`.
> - **1b (AMD-движок)** — заморожено. Разморозить только если появится
>   потребность в `medium`/`large` на скорости (напр. многоязычие,
>   диктовка), которую CPU не тянет.

Сейчас в [stt_engine.py](stt_engine.py) захардкожено `device="cpu"`. Это не
случайность и не лень: **CTranslate2 (движок faster-whisper) поддерживает
только CUDA и CPU, ROCm им не поддерживается**. То есть на NVIDIA проблема
решается одной строкой, а на AMD — нет.

### 1a. NVIDIA — дешёвая победа
Выбирать устройство по `gpu.detect_backend()`: при `cuda` передавать
`device="cuda"`, `compute_type="float16"`. Ожидаемо кратное ускорение STT.
Это надо было сделать ещё в фазе 0 (тогда мультивендорность довели только
до TTS) — долг.

### 1b. AMD — нужен другой движок STT
Варианты, в порядке предпочтения:

1. **Whisper через уже установленный ROCm-torch.** Самый прямой путь: torch
   на этой машине уже собран под ROCm и работает (F5-TTS на нём и крутится).
   Значит `transformers`-реализация Whisper (или референсный `openai-whisper`)
   пойдёт на GPU без нового рантайма. Медленнее CTranslate2 по архитектуре,
   но GPU против CPU почти наверняка перевесит. **Проверить первым.**
2. **whisper.cpp с Vulkan-бэкендом.** Vulkan работает на AMD; есть Python-
   биндинги (`pywhispercpp`). Плюс — очень лёгкий рантайм, минус — ещё одна
   нативная зависимость в сборке.
3. **ONNX Runtime + DirectML.** На Windows работает поверх любой GPU,
   включая AMD. Минус — конвертация модели в ONNX и лишний тяжёлый рантайм.

Архитектурно: ввести в `stt_engine.py` абстракцию бэкенда по аналогии с
[gpu.py](gpu.py), чтобы движок выбирался под железо, а `Assistant` про это
не знал. Тесты уже мокают `WhisperModel` — менять их не придётся.

### 1c. Модель поменьше — СДЕЛАНО (28.08.2026)
Замерены `small`/`base` (раздел 0). Дефолт `stt.model_size` переведён
`medium -> small` в `config.example.yaml` с комментарием про компромиссы.
`base` — вариант для максимальной отзывчивости на простых командах.

---

## 2. События вместо print — СДЕЛАНО (28.08.2026, ветка `v0.2-live-core`)

Было: [assistant.py](assistant.py) на любую проблему делал `print()` —
в TUI Textual перехватывает экран, в GUI вывод уходил в никуда; ответ
приходил в UI одним куском уже после того, как голос отзвучал.

Реализовано:

- **[events.py](events.py)** — `EventEmitter` + `Event` (`STATE_CHANGED`,
  `LLM_TOKEN`, `LLM_DONE`, `TOOL_STARTED/DONE`, `TTS_STARTED/DONE`,
  `STT_DONE`, `INTERRUPTED`, `ERROR`). Колбэки, не очередь: у потребителей
  уже есть свой маршалинг в UI-поток. Сломанный слушатель не роняет ядро.
- **[state.py](state.py)** — явная машина состояний
  `IDLE → LISTENING → TRANSCRIBING → THINKING → [EXECUTING_TOOL] → SPEAKING
  → IDLE`, плюс `ERROR` и barge-in из любого состояния. Нелегальный
  переход → `InvalidTransition`. (Пункт из внешнего аудита, не было в
  исходном роадмапе.)
- **`Assistant`** наследует `EventEmitter`, держит `StateMachine`, эмитит
  события по всему ходу диалога. Публичный API не изменился. Новый метод
  `interrupt()` (barge-in + сброс `StreamingSpeaker` + переход состояния).
- **[main.py](main.py)** подписан: печатает поток токенов, вызовы
  инструментов, ошибки.
- **[tui.py](tui.py)** — стриминг ответа по токенам в виджет `#streaming`,
  индикатор состояния в подзаголовке, ошибки и tool-вызовы в транскрипт.
- **[tts_engine.py](tts_engine.py)** `StreamingSpeaker` — первый кусок
  ответа режется по раннему рубежу (запятая/двоеточие/тире или предел
  60 симв.), дальше по предложениям. Первая фраза уходит в синтез, не
  дожидаясь точки — бьёт по доминирующей теперь задержке LLM TTFT.

Осталось: подписать **GUI** ([gui.py](gui.py) / `gui/index.html`) на те же
события — сейчас он ещё работает по-старому.

---

## 3. Прерываемый TTS — barge-in до конца (приоритет 1 — поднят 28.08.2026)

`TTSEngine.interrupt()` выбрасывает очередь и глушит воспроизведение, но
фразу, которую воркер уже начал синтезировать, прервать нельзя: `tts.infer()`
не поддерживает отмену. На практике — перебил ассистента, а он ещё секунду-две
договаривает.

Приоритет поднят по итогам поздней ревизии 28.08: `nfe_step` остаётся **32**
(голос важнее скорости), значит синтез одной фразы ~1.5с и worst-case
«доигрывания» после barge-in — те же ~1.5с (а не ~0.4с, как было бы при
`nfe_step=8`). Для ощущения живого разговора чистое прерывание важно не
меньше короткого TTFT.

Что делать: `StreamingSpeaker` (задача 2) уже дробит ответ на мелкие куски —
резать ещё мельче и на `interrupt()` дропать текущий буфер и невоспроизведённый
хвост. **Внутрь диффузии F5-TTS лезть не нужно** (низкий ROI, подтверждено
внешним аудитом) — достаточно гранулярности чанков.

---

## 3b. LLM time-to-first-token — новый доминирующий член задержки

Замер: gpt-oss:20b даёт TTFT 0.8–3.5с (сильно скачет — reasoning-модель), на
пути с tool-call — ~2–3с молчания перед голосом. Это теперь главный вклад в
TTFA. Что пробовать:

- прогрев модели при старте (первый вызов ~7с против ~1с на тёплой);
- `think=False` / не-reasoning модель для команд, либо более мелкая модель
  (gpt-oss меньше, qwen и т.п.) — замерить через `gogi-bench`;
- филлер-фраза («Секунду…») в синтез сразу по факту `LLM_STARTED`, не
  дожидаясь первого токена — закрывает паузу на tool-пути.

---

## 4. Таймауты и живучесть (приоритет 2)

- Нет таймаутов на запросы к Ollama: если сервис завис (не упал, а именно
  завис) — интерфейс ждёт бесконечно, без индикации.
- Нет проверки при старте, что Ollama вообще запущен и нужная модель есть.
- `switch_model()` не проверяет существование модели — ошибка вылезет только
  при следующем запросе.
- `available_models` статичен в конфиге, хотя `ollama.list()` даёт реальный
  список установленных.

---

## 5. Память диалога (приоритет 1 — поднят 28.08.2026)

`Assistant.messages` растёт без ограничений и не сохраняется между запусками:
через час разговора контекст распухнет и модель начнёт тормозить, а после
перезапуска — полная амнезия. Нужен триминг по токенам (со сохранением
system-промпта) и персистентность истории на диск.

Приоритет поднят: раздутый контекст напрямую увеличивает LLM TTFT, а он —
доминирующий член бюджета задержки (раздел 3b). То есть безлимитная история
деградирует главную цель этапа по ходу сессии, а не «когда-нибудь потом».

---

## 6. Безопасность: белый список расширений (приоритет 2)

`open_file` открывает файл через `os.startfile`, а `files.root_dir` по
умолчанию `"."` — корень проекта. Path traversal закрыт и покрыт тестами, но
`.exe`/`.bat`/`.ps1` внутри разрешённой директории откроются, то есть
выполнятся. Сейчас граница проведена по пути, а правильнее ещё и по типу
файла — добавить `files.allowed_extensions`.

---

## 7. Функциональность (приоритет 3)

- **Добавление голоса из интерфейса.** [scripts/transcribe_ref.py](scripts/transcribe_ref.py)
  умеет транскрибировать референс, но никуда не подключён. Сценарий «записать
  10 секунд с микрофона → получить новый голосовой профиль» напрашивается,
  тем более что дизайн-макет это предполагал.
- **VAD / wake word.** Сейчас только push-to-talk (клавиша/клик). Для
  «Jarvis-образа» это главное недостающее звено и самое дорогое в реализации.
  Разумный промежуточный шаг — VAD для автоматического определения конца
  фразы (тогда не нужно жать второй раз), wake word — потом.
- **Язык распознавания зашит как `ru`** — вынести в конфиг.

---

## 8. Инфраструктура (приоритет 3)

- CI гоняется только на Windows и ставит CPU-torch. Стоит добавить Linux-job
  (быстрее) и, если появится доступ, проверку CUDA-пути.
- Нет логирования в файл — диагностика проблем на чужой машине сейчас
  невозможна.
- JS в `gui/index.html` не покрыт тестами (Python-сторона покрыта).

---

## 9. Маскировка задержки — убрать мёртвую тишину (приоритет 1, новое 28.08.2026)

Контекст: пол задержки до первого звука ответа — 1.6–4.3с (раздел 0), и мы
осознанно не жмём его дальше (`nfe_step=32`, голос важнее). Значит пауза
между концом речи и первым звуком будет всегда. Задача — чтобы она не
ощущалась как «зависло», а не чтобы её убрать.

Что сделать:

- **Мгновенная акустическая квитанция** по `Event.STT_DONE` с непустым
  текстом (ещё до `LLM_STARTED`). Варианты по возрастанию риска «приесться»:
  - неречевой earcon — тихий короткий тон/вздох ~150мс, смысла не несёт,
    приедается медленнее всего. **Основной вариант.**
  - речевой бэкченел из **ротируемого пула** коротких («так…», «ага»,
    «секунду», «сейчас»), выбор случайный — не одна и та же «Секунду…»
    каждый ход (это и есть то, что задолбёт — подтверждено).
- **По условию, а не всегда.** Квитанция играет, только если первый
  осмысленный чанк от LLM не пришёл за `perceived.filler_delay_ms`
  (дефолт ~500мс). Уложился TTFT — тишины не было, филлер не нужен.
- **Не наслаивать.** Пришёл реальный первый чанк в `StreamingSpeaker` —
  отменить / не ставить филлер.
- **Текст на экран впереди звука.** `Event.LLM_TOKEN` уже стримится в TUI
  `#streaming`; проверить, что в консоли и (после перевода на события) GUI
  первый токен виден сразу — экран, опережающий звук, сам снимает ощущение
  зависания.
- Конфиг: `perceived.mode: earcon|backchannel|off`,
  `perceived.filler_delay_ms`, пул фраз.

Где трогать: [assistant.py](assistant.py) (`respond()` / `_run_turn()` —
таймер на первый `LLM_TOKEN`), [tts_engine.py](tts_engine.py) (проиграть
earcon/фразу мимо обычной очереди синтеза, чтобы не ждать F5 — заранее
синтезированный/записанный wav), `config.example.yaml`, подписчики.

Критерий готовности: на холодном пути с tool-call (worst case) пользователь
слышит квитанцию за <400мс от конца речи; на тёплом коротком ответе
(TTFT < порога) лишнего филлера нет. Тесты: мок LLM с искусственной
задержкой первого токена — (а) филлер при задержке > порога, (б) нет
филлера при задержке < порога, (в) нет наслаивания на реальный чанк.

---

## 10. Расширить замер LLM — узкое место надо мерить точно (приоритет 1, новое 28.08.2026)

Контекст: `scripts/bench_pipeline.py::bench_llm` уже меряет TTFT, но грубо:
1 прогрев (холодный TTFT выброшен), 3 прогона, только медиана/мин/макс,
путь с tool-call не выделен, 3 промпта. А это теперь доминирующий и самый
скачущий член бюджета.

Что добавить в `bench_llm`:

- **Холодный TTFT отдельной строкой** — первый вызов, когда модели нет в
  VRAM (сейчас «~7с» на глаз).
- **Путь с tool-call целиком.** Промпт с гарантированным вызовом инструмента
  («открой калькулятор»): мерить не TTFT первого ответа, а время до
  **первого произносимого токена после возврата инструмента** — те самые
  «2–3с молчания». Это два round-trip к Ollama, мерить сумму.
- **p50 и p95, не только медиана.** `--runs` для LLM поднять до ~10
  (STT/TTS оставить 3 — стабильны). Распределение — в JSON.
- **Промпты:** 3 «чистых» ответа + 3 гарантированных tool-call.
- **Ось reasoning:** `--llm-reasoning on,off` — прогнать оба, положить в
  отчёт рядом (см. задачу 11).
- `summarize_e2e`: для оценки TTFA брать **p95** LLM TTFT, не медиану —
  планируем по худшему.

Критерий готовности: `gogi-bench --llm-only` печатает холодный TTFT, тёплые
p50/p95 отдельно для чистого пути и пути с tool-call, для reasoning on/off;
`benchmarks/baseline.json` обновлён; разделы 0 и 3b переписаны по числам.

---

## 11. gpt-oss:20b без reasoning / модель поменьше (приоритет 1, новое 28.08.2026)

Контекст: gpt-oss:20b перед первым видимым токеном гонит скрытые
reasoning-токены. Если TTFT — блокер, это может быть его основной причиной.
Изменение дешёвое, бьёт прямо в доминирующий член. Раньше не пробовали.

Что сделать:

- **Отключить reasoning у gpt-oss** — через Ollama это `think=False` в
  `ollama.chat` (проверить актуальный способ для установленной версии
  gpt-oss). Прогнать задачу 10 в обоих режимах.
- **Замерить качество, не только скорость.** На наборе из задачи 12
  сравнить reasoning on/off по:
  - доля правильно выбранных инструментов и аргументов (tool-call не должен
    деградировать — это критично);
  - вменяемость текстовых ответов (ручная оценка на 10–15 репликах).
- **Кандидаты поменьше**, если reasoning-off не хватит: `qwen2.5:7b`,
  `llama3.1:8b` — те же два замера.
- Решение записать в ROADMAP: какая модель/режим — дефолт `llm.model`,
  с числами за и против. Обновить `config.example.yaml`.

Критерий готовности: таблица «модель/режим → холодный TTFT, тёплый p95 TTFT,
точность tool-call на наборе команд», выбран дефолт.

---

## 12. Тест распознавания на реальных командах (приоритет 1, новое 28.08.2026)

Контекст: вывод «STT на CPU достаточно, `base`/`small` хватает» стоит на
одном клипе 9.61с и его CER. Для команды одно неверное значимое слово =
неверный tool-call. Нужен тест на наборе, а не на одном клипе.

Что сделать:

- **Набор 30–50 коротких реплик**: команды на инструменты («открой
  калькулятор», «поставь громкость на 30 процентов», «который час»),
  имена файлов/приложений, числа + несколько «болтательных». Записать своим
  голосом в боевых условиях (тот же микрофон), сложить в
  `voices/bench_commands/` (wav + `manifest.jsonl` с эталонным текстом и
  ожидаемым tool-call). В .gitignore, как прочие замеры.
- **Метрика по делу:** не CER, а **WER по значимым словам** и — главное —
  «доля реплик, где распознавание достаточно для правильного tool-call».
  Последнее: прогнать распознанный текст через LLM с инструментами (режим
  из задачи 11), сверить инструмент + аргументы с эталоном.
- **Прогнать `base` / `small` / `medium`** на CPU, свести: модель →
  WER-значимых → доля верных tool-call → время на реплику.
- Оформить режимом `gogi-bench --stt-commands` (переиспользовать инфру
  `bench_pipeline.py`).

Критерий готовности: таблица в ROADMAP; дефолт `stt.model_size` подтверждён
или скорректирован числами с набора, а не с одного клипа.

---

## 13. Прогрев моделей на старте (приоритет 2, новое 28.08.2026)

Контекст: первый вызов F5-TTS ~7с (компиляция GPU-ядер), первый запрос к
холодной LLM тоже дорогой (~7с против ~1с тёплой). Сейчас это ест первую же
реплику каждой сессии.

Что сделать:

- Фоновый `warm_up()` (в `Assistant.__init__` или отдельным методом, который
  интерфейс зовёт после подписки на события):
  - холостой `tts.infer` на короткой фразе в никуда (не в динамик);
  - холостой `ollama.chat` на «привет» с `tools=TOOL_SCHEMA` — модель ляжет
    в VRAM;
  - опционально whisper `transcribe` на 0.5с тишины.
- Пока идёт — `State.IDLE`, но в подзаголовке «прогреваюсь…»; push-to-talk
  доступен, первая реплика подождёт конца прогрева.
- Флаг `startup.warm_up: true` (по умолчанию вкл; на слабой машине выключить).

Где трогать: [assistant.py](assistant.py), подписчики (в TUI подзаголовок
уже есть от машины состояний — добавить строку), `config.example.yaml`.

Критерий готовности: со `warm_up: true` первая реальная реплика после
запуска по TTFA не отличается от последующих (в пределах разброса).

---

## Рекомендуемый порядок

Сделано в ветке `v0.2-live-core` (28.08.2026):
- ~~бенчмарк `scripts/bench_pipeline.py`~~ → `gogi-bench`, `benchmarks/baseline.json`;
- ~~задача 1c~~ (дефолт STT `small`);
- ~~задача 2~~ (события + машина состояний + стриминг текста в TUI +
  ранняя нарезка первой фразы). GUI на события ещё не переведён.

Дальше (ревизия 28.08.2026, поздняя):

1. **Задача 10** — расширить замер LLM (холодный TTFT, путь с tool-call,
   p50/p95). Без чисел по узкому месту всё остальное про латентность —
   гадание.
2. **Задача 11** — gpt-oss без reasoning / модель поменьше. Самый дешёвый
   потенциально крупный выигрыш по TTFT; опирается на задачу 10.
3. **Задача 9** — маскировка задержки (earcon/бэкченел по условию). Даёт
   ощущение live talk даже при TTFT, который не удастся сжать.
4. **Задача 12** — тест распознавания на реальных командах. Закрывает риск
   «дефолт STT выбран по одному клипу».
5. **Задача 3** — отменяемый barge-in (приоритет поднят). Чистое прерывание —
   половина ощущения живого разговора, тем более при `nfe_step=32`.
6. **Задача 5** — триминг + персистентность контекста (приоритет поднят).
   Распухший контекст бьёт по LLM-латентности, т.е. по цели этапа.
7. **Задача 13** — прогрев на старте. Небольшая, можно взять раньше как
   передышку между крупными.
8. **GUI на события** — хвост задачи 2 (стриминг и ошибки в pywebview).
9. **VAD-эндпоинтинг** (задача 7) — как UX (убрать второе нажатие), а не как
   способ ускорить STT.
10. **1a (NVIDIA STT)** — дешёвый долг, ветка по `gpu.detect_backend()`.
11. Дальше по приоритетам: таймауты Ollama (4), whitelist расширений (6),
    логирование в файл (8).
