faster-whisper
sounddevice
numpy
soundfile
ollama
pyyaml
pywebview
torchvision
f5-tts
transformers==4.57.6
huggingface-hub==0.36.2
setuptools<81

# ВАЖНО: torch/torchaudio НЕ ставятся из этого файла. Установите их отдельно
# под свою видеокарту (NVIDIA CUDA или AMD ROCm) — см. README, раздел
# "Железо и GPU-ускорение". Иначе получите CPU-only сборку без ускорения.
# После установки любого пакета выше проверяйте torch.cuda.is_available()
# и при необходимости переустанавливайте GPU-колёса с --force-reinstall --no-deps.
